import {Typography} from "@mui/joy";

function StoreSettingsPage() {
    return (
       <Typography>
           This is the store settings page
       </Typography>
    );
}

export default StoreSettingsPage;
